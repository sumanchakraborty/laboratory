
all:
	gcc -Wall -ansi -pedantic -o winequality winequality.c -ldeep -lm -lpng -fopenmp

debug:
	gcc -Wall -ansi -pedantic -g -o winequality winequality.c -ldeep -lm -lpng -fopenmp

clean:
	rm -f *.o winequality
